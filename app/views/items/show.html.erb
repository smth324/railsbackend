<h1><%= @item.name %></h1>

<ul>
  <li><%= link_to "Destroy", item_path(@item), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<h2>Categories</h2>
<% @item.categories.each do |category| %>
<%= category.name %>
<%end%>
